# v0.2 DeepSeek 接入 + Life Wiki 架构预留 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 接入 DeepSeek 大模型生成真实成长礼物内容，同时为 Life Wiki 做架构分层预留，不做数据库/登录/云存储。

**Architecture:** 前端表单 → extractRawMaterial → POST /api/generate-report（服务端）→ deepseekClient → parseReportJson → ReportData。原始材料与生成内容分层，Life Wiki Markdown 纯浏览器导出。Skill 系统做类型预留，不拆分为多次 API 调用。

**Tech Stack:** Next.js App Router API Route（服务端，可安全读取环境变量）、fetch（调用 DeepSeek OpenAI 兼容接口）、TypeScript、浏览器 Blob 下载。

---

## 文件结构总览

### 阶段 0 — 安全基础
| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `.gitignore` | 补齐 `.env` / `.env.local` / `.env.*.local` |
| 新建 | `.env.local.example` | 变量名模板，不含真实 key |
| 修改 | `components/GrowthReportApp.tsx` | useRef 修复 photos 卸载清理 |
| 修改 | `README.md` | 换成 DeepSeek 接入说明 |

### 阶段 1 — DeepSeek 接入
| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `app/api/generate-report/route.ts` | Next.js API Route，服务端，读取 env |
| 新建 | `lib/server/deepseekClient.ts` | fetch 封装，调用 DeepSeek Chat Completions |
| 新建 | `lib/server/prompts/growthReportPrompt.ts` | RawMaterial → prompt 字符串 |
| 新建 | `lib/server/parseReportJson.ts` | 解析模型返回 JSON → ReportData |
| 新建 | `lib/aiReportGenerator.ts` | 前端 ReportGeneratorI 实现，调用 /api/generate-report |
| 修改 | `components/GrowthReportApp.tsx` | 切换到 aiGenerator，保留 mock 注释 |

### 阶段 2 — Skill 化目录预留
| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `lib/skills/README.md` | skill 化方案说明 |
| 新建 | `lib/skills/types.ts` | SkillInput / SkillOutput 基础类型 |
| 新建 | `lib/skills/reportSkillPlan.ts` | 各 skill 的设计文档和类型占位 |

### 阶段 3 — Life Wiki 导出
| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `lib/wiki/exportLifeWikiMarkdown.ts` | RawMaterial + ReportData → Markdown 字符串 |
| 新建 | `components/ExportWikiButton.tsx` | 浏览器 Blob 下载按钮 |
| 修改 | `components/ReportPreview.tsx` | 顶部操作栏加「下载 Life Wiki」按钮 |

### 阶段 4 — Graph 设计文档
| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `docs/life-wiki-graph-design.md` | 节点/边类型设计，不改运行时代码 |

### 阶段 5 — 视频脚本 Skill 预留
| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `lib/skills/videoScriptTypes.ts` | VideoScript 类型定义 |
| 新建 | `docs/video-script-skill-design.md` | 设计文档 |

---

## 阶段 0：安全基础

### Task 0-1：修复 .gitignore，确保 env 文件不被提交

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: 检查当前 .gitignore**

当前内容已有 `.env*`，覆盖了 `.env.local`，但为明确性补齐：

```
# env files
.env
.env.local
.env.*.local
.env*.local
```

确认 `.gitignore` 中 env 部分改为：

```
# env files（不提交任何 key，包括 .env.local.example 以外的所有 .env 文件）
.env
.env.local
.env.*.local
```

注意：`.env.local.example` 不在忽略列表，允许提交。

- [ ] **Step 2: 验证**

```bash
echo "TEST=abc" > .env.local
git status
```

期望：`.env.local` 出现在 `Untracked files` 但标注 ignored，不出现在待提交列表。

```bash
rm .env.local
```

- [ ] **Step 3: commit**

```bash
git add .gitignore
git commit -m "chore: 明确 .gitignore 中 env 文件规则"
```

---

### Task 0-2：新建 .env.local.example

**Files:**
- Create: `.env.local.example`

- [ ] **Step 1: 写文件**

内容：

```
# DeepSeek API 配置
# 复制本文件为 .env.local，填入真实值，不要提交 .env.local
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=
```

- [ ] **Step 2: 确认不在 .gitignore 中**

```bash
git check-ignore -v .env.local.example
```

期望：无输出（未被忽略，可以提交）。

- [ ] **Step 3: commit**

```bash
git add .env.local.example
git commit -m "chore: 新增 .env.local.example 环境变量模板"
```

---

### Task 0-3：修复 GrowthReportApp 照片 previewUrl 卸载清理（useRef 方案）

**Files:**
- Modify: `components/GrowthReportApp.tsx`

当前问题：`useEffect(() => { ... }, [])` 中的 cleanup 函数捕获了挂载时的 `formData.photos`（空数组），所以卸载时什么都不会 revoke。

修复方案：用 `useRef` 始终同步最新 photos，卸载时从 ref 读取。

- [ ] **Step 1: 修改 GrowthReportApp.tsx**

在 import 行加 `useRef`：

```typescript
import { useState, useEffect, useRef } from "react";
```

在 `handleFormChange` 之前加 ref 声明和同步：

```typescript
// 始终持有最新 photos 引用，供卸载清理使用
const photosRef = useRef(formData.photos);
useEffect(() => {
  photosRef.current = formData.photos;
}, [formData.photos]);
```

把原来的卸载 useEffect 改为：

```typescript
// 组件卸载时释放所有照片 objectURL，防止内存泄漏
useEffect(() => {
  return () => {
    photosRef.current.forEach((p) => URL.revokeObjectURL(p.previewUrl));
  };
}, []); // 空依赖：只在卸载时执行，通过 ref 拿到最新列表
```

- [ ] **Step 2: lint + build 确认无报错**

```bash
npm run lint && npm run build
```

期望：0 errors，build 成功。

- [ ] **Step 3: commit**

```bash
git add components/GrowthReportApp.tsx
git commit -m "fix: 用 useRef 修复照片 previewUrl 卸载清理内存泄漏"
```

---

### Task 0-4：更新 README 换成 DeepSeek 接入说明

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 替换「如何接入真实 AI」章节**

把 README 中的「如何接入真实 AI」章节改为：

```markdown
## 如何接入 DeepSeek

### 第一步：配置环境变量

复制模板文件：

\```bash
cp .env.local.example .env.local
\```

在 `.env.local` 中填入：

\```
DEEPSEEK_API_KEY=你的 DeepSeek API Key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
\```

**注意：`.env.local` 不会被提交到 Git，请勿分享给他人。**

### 第二步：切换到 AI 生成器

`components/GrowthReportApp.tsx` 开头：

\```typescript
// 把这两行取消注释
import { aiGenerator } from "@/lib/aiReportGenerator";
const generator = aiGenerator;

// 注释掉这行
// const generator = mockGenerator;
\```

### 第三步：启动

\```bash
npm run dev
\```

当前版本只传递文本内容给 DeepSeek，照片继续只在本地预览，不上传给 API。

### Skill 扩展点

代码中每个生成模块都有 `TODO[skill:xxx]` 标记，后续可拆为独立 AI 调用：

| 标记 | 说明 |
|------|------|
| `TODO[skill:keywords]` | 关键词提取 |
| `TODO[skill:summary]` | 成长总结 |
| `TODO[skill:timeline]` | 时间线结构化 |
| `TODO[skill:letter]` | 亲子信件 |
| `TODO[skill:social]` | 朋友圈文案 |
| `TODO[skill:video]` | 成长视频脚本（未来） |
| `TODO[skill:illustration]` | 插画提示词（未来） |
```

- [ ] **Step 2: commit**

```bash
git add README.md
git commit -m "docs: 更新 README 接入说明为 DeepSeek"
```

---

### Task 0-5：lint + build + push

- [ ] **Step 1: 全量检查**

```bash
npm run lint && npm run build
```

期望：0 errors。

- [ ] **Step 2: 确认 .env.local 不在 git 中**

```bash
git status
```

期望：不出现 `.env.local`。

- [ ] **Step 3: push**

```bash
git push origin main
```

---

## 阶段 1：接入 DeepSeek 文本生成

### Task 1-1：新建 deepseekClient.ts（服务端，仅 Node.js 环境）

**Files:**
- Create: `lib/server/deepseekClient.ts`

- [ ] **Step 1: 写文件**

```typescript
// lib/server/deepseekClient.ts
// 服务端专用，不要在客户端组件中 import 本文件
// API Key 只从环境变量读取，永远不打印到日志

type ChatMessage = {
  role: "system" | "user" | "assistant";
  content: string;
};

type DeepSeekResponse = {
  choices: { message: { content: string } }[];
};

export async function callDeepSeek(messages: ChatMessage[]): Promise<string> {
  const apiKey = process.env.DEEPSEEK_API_KEY;
  if (!apiKey) {
    throw new Error("缺少 DEEPSEEK_API_KEY，请在 .env.local 中配置");
  }

  const baseUrl = process.env.DEEPSEEK_BASE_URL ?? "https://api.deepseek.com";
  const model = process.env.DEEPSEEK_MODEL;
  if (!model) {
    throw new Error("缺少 DEEPSEEK_MODEL，请在 .env.local 中配置");
  }

  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), 60_000); // 60s 超时

  let res: Response;
  try {
    res = await fetch(`${baseUrl}/v1/chat/completions`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${apiKey}`,
      },
      body: JSON.stringify({
        model,
        messages,
        temperature: 0.7,
        max_tokens: 4096,
        response_format: { type: "json_object" },
      }),
      signal: controller.signal,
    });
  } catch (err) {
    if ((err as Error).name === "AbortError") {
      throw new Error("请求超时（60秒），请稍后重试");
    }
    throw new Error(`网络请求失败：${(err as Error).message}`);
  } finally {
    clearTimeout(timeoutId);
  }

  if (!res.ok) {
    const body = await res.text().catch(() => "");
    throw new Error(`DeepSeek API 返回错误 ${res.status}：${body.slice(0, 200)}`);
  }

  const data = (await res.json()) as DeepSeekResponse;
  const content = data.choices?.[0]?.message?.content;
  if (!content) {
    throw new Error("DeepSeek 返回内容为空");
  }
  return content;
}
```

- [ ] **Step 2: build 确认无 TS 错误**

```bash
npm run build
```

- [ ] **Step 3: commit**

```bash
git add lib/server/deepseekClient.ts
git commit -m "feat: 新增 deepseekClient 服务端封装"
```

---

### Task 1-2：新建 growthReportPrompt.ts

**Files:**
- Create: `lib/server/prompts/growthReportPrompt.ts`

- [ ] **Step 1: 写文件**

```typescript
// lib/server/prompts/growthReportPrompt.ts
import { RawMaterial } from "@/lib/types";

const STYLE_DESCRIPTIONS: Record<string, string> = {
  warm: "温暖细腻，像妈妈写给孩子的日记，充满柔软的爱意",
  playful: "俏皮活泼，有童趣，像从孩子视角看世界",
  documentary: "真实深情，克制但有力，像一份认真的成长档案",
  literary: "诗意悠远，有文学质感，像写给未来的一封长信",
};

export function buildGrowthReportPrompt(material: RawMaterial): string {
  const styleName = STYLE_DESCRIPTIONS[material.style] ?? STYLE_DESCRIPTIONS.warm;
  const age = material.childAge !== "" ? `${material.childAge} 岁` : "（年龄未填）";

  const qaSection =
    material.qaList.length > 0
      ? material.qaList
          .map((qa, i) => `问题${i + 1}：${qa.question}\n回答：${qa.answer}`)
          .join("\n\n")
      : "（用户未填写访谈问答）";

  const freeNoteSection = material.freeNote.trim()
    ? `\n\n【自由记录 / 日记原文】\n${material.freeNote}`
    : "";

  return `你是一位专业的家庭记忆整理师和儿童成长记录编辑。你的工作是帮助年轻父母，把他们提供的真实材料，整理成一份孩子长大后会珍藏的成长礼物。

【重要原则】
1. 必须严格基于用户提供的材料，禁止编造用户没有提到的具体事件、地点、疾病、人物、成绩。
2. 如果某个字段信息不足，可以做温和的总结性表达，但绝不虚构细节。
3. 文风要${styleName}。
4. 整体基调：温暖、真实、克制，不要油腻、不要煽情过度。
5. 面向读者：这份内容未来会被孩子本人读到，要经得起时间考验。

【孩子信息】
- 昵称：${material.childName}
- 年龄：${age}
- 总结年份：${material.reportYear} 年
- 父母称呼：${material.parentName}

【父母的回答】
${qaSection}${freeNoteSection}

【输出要求】
请输出严格的 JSON，不要输出 Markdown，不要输出代码块，不要输出任何解释性文字，直接输出 JSON 对象。

JSON 结构如下：
{
  "title": "孩子昵称 + 年份 + 成长礼物，例如：小熊宝的 2024 成长礼物",
  "keywords": ["关键词1", "关键词2", "关键词3", "关键词4", "关键词5"],
  "yearlySummary": "3-4 段，每段用\\n\\n分隔，基于用户填写内容的年度成长总结",
  "timeline": [
    { "time": "时间描述，如3月、暑假、年底", "title": "简短事件标题", "description": "1-2句事件描述" }
  ],
  "letter": "父母写给孩子的一封信，署名用父母称呼，结尾标注年份",
  "socialPosts": [
    { "title": "温暖版", "content": "适合发朋友圈的温暖文案，含话题标签" },
    { "title": "走心版", "content": "更深情的文案，适合配照片" },
    { "title": "简洁版", "content": "简短有力的版本" }
  ]
}

要求：
- keywords：3-5 个，从材料中提炼，不要泛泛的词
- yearlySummary：至少 200 字，基于真实回答内容
- timeline：3-5 条，从用户的回答中提取真实事件，信息不足时可以做季节性总结
- letter：至少 200 字，有温度，不煽情，对孩子说话的口吻
- socialPosts：3 条，每条风格不同`;
}
```

- [ ] **Step 2: build**

```bash
npm run build
```

- [ ] **Step 3: commit**

```bash
git add lib/server/prompts/growthReportPrompt.ts
git commit -m "feat: 新增 growthReportPrompt 构建成长礼物 prompt"
```

---

### Task 1-3：新建 parseReportJson.ts

**Files:**
- Create: `lib/server/parseReportJson.ts`

- [ ] **Step 1: 写文件**

```typescript
// lib/server/parseReportJson.ts
import { ReportData, TimelineItem, SocialPost } from "@/lib/types";

const DEFAULT_KEYWORDS = ["认真成长", "被爱包围", "好奇世界", "温暖瞬间", "珍贵记忆"];

const DEFAULT_TIMELINE: TimelineItem[] = [
  { time: "春天", title: "新的一年开始了", description: "在家人的陪伴下，迎来新的一岁。" },
  { time: "夏天", title: "努力成长中", description: "炎热的夏天，学到了很多新东西。" },
  { time: "秋天", title: "丰收的季节", description: "秋天的风里，留下了很多珍贵的瞬间。" },
];

const DEFAULT_SOCIAL_POSTS: SocialPost[] = [
  { title: "温暖版", content: "孩子又长大了一岁，每一年都舍不得让它就这么过去。\n\n#成长记录 #给孩子的礼物" },
  { title: "走心版", content: "有一天，孩子会看见自己是如何被爱着长大的。这是我们给你的礼物。\n\n#致未来的你" },
  { title: "简洁版", content: "记录这一年，留给未来的你。🎁" },
];

// 从模型返回的文本中提取 JSON（兼容偶尔包裹 ```json 的情况）
function extractJson(raw: string): string {
  const trimmed = raw.trim();
  // 去掉可能的 ```json ... ``` 包裹
  const codeBlockMatch = trimmed.match(/^```(?:json)?\s*([\s\S]*?)\s*```$/);
  if (codeBlockMatch) return codeBlockMatch[1];
  return trimmed;
}

export function parseReportJson(raw: string, childName: string, reportYear: number): ReportData {
  let parsed: Record<string, unknown>;

  try {
    parsed = JSON.parse(extractJson(raw));
  } catch {
    throw new Error("AI 返回格式解析失败，请重试");
  }

  // 安全取值辅助
  const str = (v: unknown, fallback = ""): string =>
    typeof v === "string" && v.trim() !== "" ? v : fallback;

  const strArr = (v: unknown, fallback: string[]): string[] => {
    if (Array.isArray(v) && v.length > 0) return v.map(String);
    return fallback;
  };

  // keywords：至少 3 个
  let keywords = strArr(parsed.keywords, DEFAULT_KEYWORDS);
  if (keywords.length < 3) keywords = [...keywords, ...DEFAULT_KEYWORDS].slice(0, 5);

  // timeline：至少 3 条
  let timeline: TimelineItem[] = DEFAULT_TIMELINE;
  if (Array.isArray(parsed.timeline) && parsed.timeline.length >= 3) {
    timeline = (parsed.timeline as unknown[]).map((item) => {
      const t = item as Record<string, unknown>;
      return {
        time: str(t.time, "某个时刻"),
        title: str(t.title, "成长瞬间"),
        description: str(t.description, ""),
      };
    });
  }

  // socialPosts：至少 3 条
  let socialPosts: SocialPost[] = DEFAULT_SOCIAL_POSTS;
  if (Array.isArray(parsed.socialPosts) && parsed.socialPosts.length >= 3) {
    socialPosts = (parsed.socialPosts as unknown[]).map((item) => {
      const p = item as Record<string, unknown>;
      return {
        title: str(p.title, "版本"),
        content: str(p.content, ""),
      };
    });
  }

  return {
    title: str(parsed.title, `${childName}的 ${reportYear} 成长礼物`),
    keywords,
    yearlySummary: str(parsed.yearlySummary, `${reportYear} 年，${childName} 又长大了一岁。`),
    timeline,
    letter: str(parsed.letter, `亲爱的 ${childName}，这是 ${reportYear} 年，爸爸妈妈写给你的话。`),
    socialPosts,
    skillStatus: {
      keywords: "done",
      yearlySummary: "done",
      timeline: "done",
      letter: "done",
      socialPosts: "done",
    },
  };
}
```

- [ ] **Step 2: build**

```bash
npm run build
```

- [ ] **Step 3: commit**

```bash
git add lib/server/parseReportJson.ts
git commit -m "feat: 新增 parseReportJson 解析模型返回 JSON"
```

---

### Task 1-4：新建 API Route

**Files:**
- Create: `app/api/generate-report/route.ts`

- [ ] **Step 1: 写文件**

```typescript
// app/api/generate-report/route.ts
// 服务端 API Route，Next.js App Router
// 只允许 POST，API Key 只在服务端读取，前端不可见

import { NextRequest, NextResponse } from "next/server";
import { RawMaterial } from "@/lib/types";
import { callDeepSeek } from "@/lib/server/deepseekClient";
import { buildGrowthReportPrompt } from "@/lib/server/prompts/growthReportPrompt";
import { parseReportJson } from "@/lib/server/parseReportJson";

export async function POST(req: NextRequest) {
  let material: RawMaterial;
  try {
    material = (await req.json()) as RawMaterial;
  } catch {
    return NextResponse.json({ error: "请求体解析失败" }, { status: 400 });
  }

  // 校验必填字段
  if (!material.childName?.trim()) {
    return NextResponse.json({ error: "缺少孩子昵称" }, { status: 400 });
  }
  if (material.childAge === "" || material.childAge === undefined) {
    return NextResponse.json({ error: "缺少孩子年龄" }, { status: 400 });
  }
  if (!material.parentName?.trim()) {
    return NextResponse.json({ error: "缺少父母称呼" }, { status: 400 });
  }
  if (!material.qaList || material.qaList.length < 2) {
    return NextResponse.json({ error: "至少需要回答 2 个问题" }, { status: 400 });
  }

  try {
    const prompt = buildGrowthReportPrompt(material);
    const raw = await callDeepSeek([
      { role: "system", content: "你是专业的家庭记忆整理师，输出严格 JSON，不要输出任何其他内容。" },
      { role: "user", content: prompt },
    ]);

    const report = parseReportJson(
      raw,
      material.childName,
      material.reportYear
    );

    return NextResponse.json(report);
  } catch (err) {
    const message = err instanceof Error ? err.message : "生成失败，请重试";
    return NextResponse.json({ error: message }, { status: 500 });
  }
}

// 拒绝非 POST 请求
export async function GET() {
  return NextResponse.json({ error: "Method Not Allowed" }, { status: 405 });
}
```

- [ ] **Step 2: build**

```bash
npm run build
```

- [ ] **Step 3: commit**

```bash
git add app/api/generate-report/route.ts
git commit -m "feat: 新增 /api/generate-report API Route"
```

---

### Task 1-5：新建 aiReportGenerator.ts（前端调用层）

**Files:**
- Create: `lib/aiReportGenerator.ts`

- [ ] **Step 1: 写文件**

```typescript
// lib/aiReportGenerator.ts
// 前端 ReportGeneratorI 实现
// 只调用 /api/generate-report，不读取任何环境变量
import { ReportGeneratorI, RawMaterial, ReportData } from "./types";

class AiReportGenerator implements ReportGeneratorI {
  async generate(material: RawMaterial): Promise<ReportData> {
    const res = await fetch("/api/generate-report", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      // 照片 previewUrl 是 blob:// URL，对服务端无意义，发送前移除
      body: JSON.stringify({ ...material, photoUrls: [] }),
    });

    const data = await res.json() as ReportData & { error?: string };

    if (!res.ok || data.error) {
      throw new Error(data.error ?? `服务器错误 ${res.status}`);
    }

    return data;
  }
}

export const aiGenerator = new AiReportGenerator();
```

- [ ] **Step 2: build**

```bash
npm run build
```

- [ ] **Step 3: commit**

```bash
git add lib/aiReportGenerator.ts
git commit -m "feat: 新增 aiReportGenerator 前端调用层"
```

---

### Task 1-6：切换 GrowthReportApp 到 aiGenerator

**Files:**
- Modify: `components/GrowthReportApp.tsx`

- [ ] **Step 1: 修改 import 和 generator 声明**

把文件顶部：

```typescript
import { mockGenerator } from "@/lib/mockReportGenerator";
// TODO[ai-api]: 接入真实 AI 时，替换为：
// import { aiGenerator } from "@/lib/aiReportGenerator";
// const generator = aiGenerator;
const generator = mockGenerator;
```

改为：

```typescript
import { aiGenerator } from "@/lib/aiReportGenerator";
// 本地无 API Key 时切换回 mock：
// import { mockGenerator } from "@/lib/mockReportGenerator";
// const generator = mockGenerator;
const generator = aiGenerator;
```

- [ ] **Step 2: lint + build**

```bash
npm run lint && npm run build
```

期望：0 errors。

- [ ] **Step 3: 手动验收（需要 .env.local）**

无 `.env.local`：点击生成 → 显示「缺少 DEEPSEEK_API_KEY」错误提示。

有 `.env.local` 并填入真实 key：点击生成 → 约 5-15 秒 → 进入结果页，内容为 AI 生成。

- [ ] **Step 4: commit + push**

```bash
git add components/GrowthReportApp.tsx
git commit -m "feat: 切换到 aiGenerator，接入 DeepSeek 生成"
git push origin main
```

---

## 阶段 2：Skill 化目录预留

### Task 2-1：新建 lib/skills/ 目录结构

**Files:**
- Create: `lib/skills/README.md`
- Create: `lib/skills/types.ts`
- Create: `lib/skills/reportSkillPlan.ts`

- [ ] **Step 1: 写 lib/skills/README.md**

```markdown
# Skills 目录

本目录用于未来将生成流程拆分为独立 Skill 的准备工作。

## 当前状态（v0.2）

一次 API 调用生成完整 ReportData，所有内容在 `lib/server/prompts/growthReportPrompt.ts` 中一起构建。

## 未来 Skill 化方案

每个 skill 是一个独立的 AI 调用单元，有独立 prompt、独立输出类型、独立重试机制。

| Skill | 输入 | 输出 | 状态 |
|-------|------|------|------|
| keywordSkill | RawMaterial | string[] | 设计预留 |
| summarySkill | RawMaterial | string | 设计预留 |
| timelineSkill | RawMaterial | TimelineItem[] | 设计预留 |
| letterSkill | RawMaterial | string | 设计预留 |
| socialPostSkill | RawMaterial | SocialPost[] | 设计预留 |
| videoScriptSkill | RawMaterial + ReportData | VideoScript | 设计预留 |
| wikiSourceSkill | RawMaterial + ReportData | WikiSource | 设计预留 |

## 为什么现在不拆分

当前每次生成是一次用户主动触发的行为。拆成多次调用会：
1. 增加总延迟（串行）
2. 增加部分失败的复杂度
3. 增加 API 费用

等用户量增长、需要精细控制各模块质量时再拆。
```

- [ ] **Step 2: 写 lib/skills/types.ts**

```typescript
// lib/skills/types.ts
// Skill 系统基础类型预留
// 当前 v0.2 未实际使用，为未来拆分做类型准备

import { RawMaterial, ReportData } from "@/lib/types";

// 每个 Skill 的标准接口
export interface Skill<TInput, TOutput> {
  name: string;
  run(input: TInput): Promise<TOutput>;
}

// 各 Skill 的输入/输出类型别名（方便未来调整）
export type KeywordSkillInput = Pick<RawMaterial, "childName" | "childAge" | "reportYear" | "qaList" | "freeNote">;
export type KeywordSkillOutput = string[];

export type SummarySkillInput = RawMaterial;
export type SummarySkillOutput = string;

export type TimelineSkillInput = RawMaterial;
export type TimelineSkillOutput = ReportData["timeline"];

export type LetterSkillInput = RawMaterial;
export type LetterSkillOutput = string;

export type SocialPostSkillInput = RawMaterial;
export type SocialPostSkillOutput = ReportData["socialPosts"];

// Life Wiki source（未来拆分时使用）
export type WikiSourceInput = { rawMaterial: RawMaterial; report: ReportData };
export type WikiSourceOutput = {
  markdown: string;
  suggestedLinks: string[];
};
```

- [ ] **Step 3: 写 lib/skills/reportSkillPlan.ts**

```typescript
// lib/skills/reportSkillPlan.ts
// Skill 化执行计划占位
// 当前 v0.2 中此文件仅作设计文档用途，不参与运行时

/**
 * 未来 Skill 化执行顺序设计（串行或并行待定）：
 *
 * Phase 1（可并行）:
 *   - keywordSkill(rawMaterial)     → keywords
 *   - summarySkill(rawMaterial)     → yearlySummary
 *   - timelineSkill(rawMaterial)    → timeline
 *
 * Phase 2（依赖 Phase 1 结果）:
 *   - letterSkill(rawMaterial)      → letter
 *   - socialPostSkill(rawMaterial)  → socialPosts
 *
 * Phase 3（依赖完整 ReportData）:
 *   - wikiSourceSkill({ rawMaterial, report }) → WikiSource
 *   - videoScriptSkill({ rawMaterial, report }) → VideoScript（可选）
 *
 * 当前 v0.2：以上全部在一次 callDeepSeek() 中完成。
 */

export const SKILL_EXECUTION_PLAN = {
  version: "0.2",
  mode: "single-call" as const,
  // 升级到 multi-skill 时改为 "parallel" | "sequential"
  futureMode: "parallel" as const,
};
```

- [ ] **Step 4: build**

```bash
npm run build
```

- [ ] **Step 5: commit + push**

```bash
git add lib/skills/
git commit -m "feat: 新增 skills 目录，预留 skill 化类型和执行计划"
git push origin main
```

---

## 阶段 3：Life Wiki 导出雏形

### Task 3-1：新建 exportLifeWikiMarkdown.ts

**Files:**
- Create: `lib/wiki/exportLifeWikiMarkdown.ts`

- [ ] **Step 1: 写文件**

```typescript
// lib/wiki/exportLifeWikiMarkdown.ts
// 纯函数：根据 rawMaterial + report 生成 Life Wiki Markdown 字符串
// 只做浏览器下载，不做服务器持久化，不做数据库

import { RawMaterial, ReportData } from "@/lib/types";

export function exportLifeWikiMarkdown(
  rawMaterial: RawMaterial,
  report: ReportData
): string {
  const now = new Date().toISOString().split("T")[0];
  const name = rawMaterial.childName || "宝贝";
  const year = rawMaterial.reportYear;

  const frontmatter = `---
type: growth_source
childName: ${name}
year: ${year}
createdAt: ${now}
tags:
  - growth
  - family-memory
  - ${year}
---`;

  const basicInfo = [
    `- **孩子昵称**：${name}`,
    `- **年龄**：${rawMaterial.childAge !== "" ? rawMaterial.childAge + " 岁" : "未填写"}`,
    `- **总结年份**：${year} 年`,
    `- **父母称呼**：${rawMaterial.parentName}`,
    `- **文案风格**：${rawMaterial.style}`,
  ].join("\n");

  const qaSection =
    rawMaterial.qaList.length > 0
      ? rawMaterial.qaList
          .map((qa, i) => `**Q${i + 1}：${qa.question}**\n\n${qa.answer}`)
          .join("\n\n---\n\n")
      : "（未填写）";

  const freeNoteSection = rawMaterial.freeNote.trim() || "（未填写）";

  const keywordsSection = report.keywords.map((kw) => `- ${kw}`).join("\n");

  const timelineSection = report.timeline
    .map((item) => `**${item.time}** · ${item.title}\n\n${item.description}`)
    .join("\n\n---\n\n");

  const socialPostsSection = report.socialPosts
    .map((post) => `### ${post.title}\n\n${post.content}`)
    .join("\n\n");

  const wikiLinks = [
    `[[${name}]]`,
    `[[${year}]]`,
    `[[成长礼物]]`,
    `[[家庭记忆]]`,
    `[[${name}的${year}]]`,
  ]
    .map((link) => `- ${link}`)
    .join("\n");

  return `${frontmatter}

# ${name} 的 ${year} 成长礼物

## 原始材料

### 基本信息

${basicInfo}

### 访谈问答

${qaSection}

### 自由记录

${freeNoteSection}

## AI 整理内容

### 年度关键词

${keywordsSection}

### 成长总结

${report.yearlySummary}

### 重要瞬间时间线

${timelineSection}

### 写给孩子的信

${report.letter}

### 朋友圈文案

${socialPostsSection}

## Wiki 链接建议

${wikiLinks}
`;
}
```

- [ ] **Step 2: build**

```bash
npm run build
```

- [ ] **Step 3: commit**

```bash
git add lib/wiki/exportLifeWikiMarkdown.ts
git commit -m "feat: 新增 exportLifeWikiMarkdown 生成 Life Wiki Markdown"
```

---

### Task 3-2：新建 ExportWikiButton 组件

**Files:**
- Create: `components/ExportWikiButton.tsx`

- [ ] **Step 1: 写文件**

```typescript
// components/ExportWikiButton.tsx
"use client";

import { RawMaterial, ReportData } from "@/lib/types";
import { exportLifeWikiMarkdown } from "@/lib/wiki/exportLifeWikiMarkdown";

type Props = {
  rawMaterial: RawMaterial;
  report: ReportData;
};

export default function ExportWikiButton({ rawMaterial, report }: Props) {
  function handleExport() {
    const markdown = exportLifeWikiMarkdown(rawMaterial, report);
    const filename = `${rawMaterial.childName || "宝贝"}-${rawMaterial.reportYear}-成长礼物.md`;

    const blob = new Blob([markdown], { type: "text/markdown;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = filename;
    a.click();
    URL.revokeObjectURL(url);
  }

  return (
    <button
      onClick={handleExport}
      className="px-3 py-1.5 rounded-full text-xs font-medium cursor-pointer transition-all hover:opacity-90 active:scale-95"
      style={{ background: "var(--accent)", color: "var(--primary)", border: "1px solid var(--border)" }}
    >
      📥 导出 Life Wiki
    </button>
  );
}
```

- [ ] **Step 2: build**

```bash
npm run build
```

- [ ] **Step 3: commit**

```bash
git add components/ExportWikiButton.tsx
git commit -m "feat: 新增 ExportWikiButton 组件"
```

---

### Task 3-3：在 ReportPreview 顶部操作栏加入导出按钮

**Files:**
- Modify: `components/ReportPreview.tsx`

- [ ] **Step 1: 在文件顶部 import ExportWikiButton**

在 `components/ReportPreview.tsx` 第 4 行（import PrintButton 之后）加：

```typescript
import ExportWikiButton from "./ExportWikiButton";
```

- [ ] **Step 2: 把 ExportWikiButton 加入顶部操作栏**

在 ReportPreview 组件的顶部操作栏，`<PrintButton />` 之前插入：

```tsx
{tab === "generated" && (
  <ExportWikiButton rawMaterial={rawMaterial} report={report} />
)}
```

完整操作栏 div 内容变为（`tab === "generated"` 时才显示导出按钮）：

```tsx
<div className="flex items-center gap-2">
  {(["generated", "raw"] as Tab[]).map((t) => (
    <button key={t} onClick={() => setTab(t)}
      className="px-3 py-1.5 rounded-full text-xs font-medium cursor-pointer transition-all"
      style={tab === t
        ? { background: "var(--primary)", color: "white" }
        : { background: "var(--accent)", color: "var(--text-muted)", border: "1px solid var(--border)" }
      }>
      {t === "generated" ? "✨ 年报" : "📋 原始记录"}
    </button>
  ))}
  {tab === "generated" && (
    <ExportWikiButton rawMaterial={rawMaterial} report={report} />
  )}
  <PrintButton />
</div>
```

- [ ] **Step 3: lint + build**

```bash
npm run lint && npm run build
```

- [ ] **Step 4: commit + push**

```bash
git add components/ReportPreview.tsx
git commit -m "feat: 在结果页顶部加入 Life Wiki 导出按钮"
git push origin main
```

---

## 阶段 4：Life Wiki Graph 设计文档

### Task 4-1：写设计文档

**Files:**
- Create: `docs/life-wiki-graph-design.md`

- [ ] **Step 1: 写文件（见下方内容）**

文件内容见下方完整 Markdown，涵盖目录结构、节点类型、边类型、抽取方式、Obsidian 兼容说明。

- [ ] **Step 2: commit + push**

```bash
git add docs/life-wiki-graph-design.md
git commit -m "docs: Life Wiki Graph 设计文档"
git push origin main
```

---

## 阶段 5：视频脚本 Skill 设计预留

### Task 5-1：写 VideoScript 类型和设计文档

**Files:**
- Create: `lib/skills/videoScriptTypes.ts`
- Create: `docs/video-script-skill-design.md`

- [ ] **Step 1: 写 lib/skills/videoScriptTypes.ts**

```typescript
// lib/skills/videoScriptTypes.ts
// 视频脚本 Skill 类型预留
// 当前 v0.2 未实现，供未来接入剪映/CapCut 或视频生成 API 使用

export type VideoSceneDuration = "30s" | "60s" | "90s";

export type VideoScene = {
  order: number;
  visualSuggestion: string; // 画面建议，例如：展示孩子第一次走路的照片
  narration: string;        // 画外音文案
  subtitle: string;         // 字幕文案（可比 narration 更简短）
  emotion: string;          // 情绪基调，例如：温暖、感动、俏皮
};

export type VideoScript = {
  title: string;
  duration: VideoSceneDuration;
  scenes: VideoScene[];
  musicMood: string;   // 背景音乐建议，例如：轻柔钢琴曲、欢快儿歌
  endingLine: string;  // 结尾字幕文案
};
```

- [ ] **Step 2: build**

```bash
npm run build
```

- [ ] **Step 3: commit + push**

```bash
git add lib/skills/videoScriptTypes.ts docs/video-script-skill-design.md
git commit -m "feat: 新增视频脚本 Skill 类型预留和设计文档"
git push origin main
```

---

## 自检清单

- [ ] `.env.local` 不在 git 中
- [ ] 代码中无硬编码 API Key
- [ ] `DEEPSEEK_API_KEY` 只在服务端 `lib/server/deepseekClient.ts` 中读取
- [ ] 前端 `lib/aiReportGenerator.ts` 不读取任何 `process.env`
- [ ] `photoUrls` 在发送给服务端前被清空（blob:// URL 对服务端无意义且暴露本地路径）
- [ ] 无 `.env.local` 时点击生成显示清晰错误
- [ ] lint + build 全通过
- [ ] 所有阶段代码都已 push 到 GitHub
